<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.drone.repository.RoleRepository">
    <resultMap id="roleMap" type="com.example.drone.domain.entity.SysRole">
        <id property="id" column="id"/>
        <result property="roleCode" column="role_code"/>
        <result property="roleName" column="role_name"/>
        <result property="description" column="description"/>
    </resultMap>
    <select id="selectByCode" resultMap="roleMap">SELECT * FROM sys_role WHERE role_code = #{roleCode}</select>
    <select id="selectByUserId" resultMap="roleMap">
        SELECT r.* FROM sys_role r INNER JOIN sys_user_role ur ON r.id = ur.role_id WHERE ur.user_id = #{userId}
    </select>
</mapper>
