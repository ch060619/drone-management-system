<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.drone.repository.OperationLogRepository">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_operation_log (user_id, operation, target, ip, op_time, result)
        VALUES (#{userId}, #{operation}, #{target}, #{ip}, NOW(), #{result})
    </insert>
</mapper>
